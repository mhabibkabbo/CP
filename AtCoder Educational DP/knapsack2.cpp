#include <bits/stdc++.h>
using namespace std;

typedef long long ll;

int main(){
    ios::sync_with_stdio(false);
    cin.tie(0);

    // Basically the problem is same as the first knapsack problem
    // but in this problem W <= 10^9, so you can't take a 2D array
    // the only modification we need is to optimize the memory here
    
    
    return 0;
}